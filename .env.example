# Copy to .env.local and fill in — never commit real keys.
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.0-flash
# Future http inbox server source for the pipeline (loader.py supports it):
# INBOX_SOURCE=http://localhost:8080